{
  "name": "gaze-pause",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "視線(顔向き)検出でYouTube/Prime Videoを自動一時停止するChrome拡張機能",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest",
    "assets:fetch": "node scripts/fetch-mediapipe-assets.mjs",
    "postinstall": "node scripts/fetch-mediapipe-assets.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.2",
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.0"
  }
}
